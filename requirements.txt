# Core
python-dotenv>=1.0
pydantic>=2.0
pydantic-settings>=2.0
numpy>=1.24
requests>=2.31

# LlamaIndex (RAG framework - Document, Readers, Splitter)
llama-index-core>=0.10.0
llama-index-readers-file>=0.1.0
pypdf>=4.0

# Embeddings (HuggingFace via FastEmbed - ONNX, no torch required)
fastembed>=0.3.0

# Vector DB
qdrant-client>=1.9.0

# Hybrid search + rerank
rank-bm25>=0.2.2
# cross-encoder is optional (needs torch); fallback works without it
# sentence-transformers>=2.2
# torch -- install manually if you want CrossEncoder rerank

# API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# UI
streamlit>=1.33
